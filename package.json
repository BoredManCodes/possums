{
  "name": "possums",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted baby tracker — feeds, nappies, naps. Runs on Cloudflare Workers + KV.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
